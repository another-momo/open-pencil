# T74 核验 · dev 桥启动时序 race（startMCPRuntime 退避重试）

> 日期：2026-09-02。独立核验 subagent（只读，未改动任何实现文件；git 仅读操作 status/diff/log/show）。
> 材料：T74-plan.md（根因实证 + 方案）、T74-self-check.md（自检申报）。
> 实现：src/app/automation/mcp/runtime.ts；测试：tests/engine/rebuild/mcp/runtime-startup-race.test.ts（新）、tests/engine/app/automation/mcp-runtime.test.ts（改）；登记：tools/zone-registry/zones.json P141/P142。

## 逐项核验

| 项                       | 结论 | 证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------------ | ---- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| V1 根因对症              | PASS | plan §1 的 race 在码中成立：vite-plugin.ts `configureServer` L236 `await enqueue(startChild)`，startChild（L151-198）`spawn('bun', ['run','packages/mcp/src/index.ts'])` 异步起子进程，listen 7600 需时间；spawn.ts `configureDevMCP`（L357-380）POST `/__openpencil/mcp/restart` 后 `pollHealth(10, 250)`（仅 2.5s 窗口）拿不到健康即 throw。git diff HEAD 证实旧 startOperation 为单次 try/catch——失败即 disconnectCurrentServer + status='error' 弃连；新代码（runtime.ts L131-149）改为 `for (attempt = 0; attempt <= MCP_STARTUP_RETRY_DELAYS_MS.length; attempt++)` 退避重试循环，旧「单次失败即弃连」路径确被取代。 |
| V2 重试语义              | PASS | 总尝试 = 1 + delays.length：循环上界 `attempt <= length`（L131）→ 6 次尝试 = 1+5。成功路径（L133-141）：健康检查通过后 connect 恰一次（L137-138，`activeStore && canConnect()` 内联在成功分支）→ applyHealth → 立即 return，connect 不可能二次触发。全失败收口：disconnectCurrentServer 恰一次（L151），status='error'（L152），console.warn（L156）。中途 state.error 逐次更新（L144），成功时 applyHealth 清零（L76-81 `state.error = null`）。sleep 只在两次尝试之间（L145-147 `attempt < length`，末次失败后不再睡）。间隔序列 200+500+1000+2000+4000 = 7700ms，与「总窗口 ≤7.7s」申报一致。                    |
| V3 测试钉扎真实有效      | PASS | unpiped 复跑 `bun test tests/engine/rebuild/mcp/runtime-startup-race.test.ts tests/engine/app/automation/mcp-runtime.test.ts` → **9 pass / 0 fail / 34 expects**，与自检申报逐字一致。断言与实现对照非自欺：用例①（race test L66-80）failHealthTimes=2 → spawn/readHealth 各 3 次、connect 恰 1、sleeps=[200,500]（成功即停）——与 L131-147 控制流吻合；用例②（L82-95）全失败 → spawn 恰 6 次（=1+DELAYS.length）、connect 0、sleeps 全序列、error='MCP server did not become healthy'——与 L135/L150-155 吻合；用例③（L97-99）逐值钉扎 [200,500,1000,2000,4000] = runtime.ts L46 常量。                                             |
| V4 门禁复跑（unpiped）   | PASS | `bun run lint` exit 0（0 errors / 7 warnings，抽查均为 packages/core 等既有文件 max-lines，与 T74 文件无关）；`bunx tsgo --noEmit` exit 0；`bun run format:check` exit 0（2166 files all correct）；`bun run check:zones` exit 0（clean：85 modified all registered / 544 added owned）；`bun run check:i18n` exit 0（in sync）；`bun run check:docs` exit 0（44/44）。未跑全量 bun test（遵守 owner 明令）。                                                                                                                                                                                                              |
| V5 zones 登记            | PASS | zones.json P141 → src/app/automation/mcp/runtime.ts（task T74，reason 述 race 根因 + 退避重试 + sleep 注入点，与改动一致）；P142 → tests/engine/app/automation/mcp-runtime.test.ts（task T74，reason 述零延迟 sleep 防 7.7s 超 bun 5s 超时，与 diff 一致）。git status --porcelain：T74 新文件 = tests/engine/rebuild/mcp/runtime-startup-race.test.ts（∈ ownedRoot tests/engine/rebuild/）+ T74-plan/self-check（∈ ownedRoot docs/），无漏登；check:zones clean 佐证。                                                                                                                                                 |
| V6 自检申报属实          | PASS | §2 浏览器实证的代码路径成立：WorkspaceView.vue onMounted L68-70 `await startMCPRuntime(getActiveStore)`（L7 import）→ runtime.ts start → startOperation 新重试循环——fresh dev 下桥慢起时不再需要手动救连，「status ok / running」结论在路径上可信（浏览器实测本身未由核验员复跑，属 owner 侧实证）。§3 门禁清单与本核验员复跑结果全一致（9 pass / lint 0 err / tsgo 0 / format 0 / zones clean / i18n sync / docs 44/44）。§4 偏差 4 条均属实：①全量弃权与给我核验指令中的同一 owner 明令吻合；②lint 三修在码中可指——runtime.ts L124-127 setTimeout executor 块体（no-promise-executor-return）、race test L17 `noop = () => undefined`（no-empty-function）、两测试 `({}) as never` getStore（no-broad-double-cast）；③P142 确为计划外登记（plan §3 只列 P141）；④vite-plugin.ts configureServer L236 startChild + L227 restartChild 双路径与「日志 3 次打印」残留观察相容，且 plan §4 明列不改 spawn 时机。 |
| V7 边界                  | PASS | 核验时点 git diff HEAD --stat：`M` runtime.ts / mcp-runtime.test.ts / zones.json 三件 + untracked T74 四件（plan、self-check、race 测试、另两份 2026-09-01 review 记录属非 T74 既有遗留，见补充观察）。`git diff HEAD --stat -- vite/ packages/mcp/ src/app/automation/mcp/spawn.ts` 为空——vite-plugin.ts、spawn.ts、桥 packages/mcp 零改动，符合 plan §4 边界；restart/refresh/stop 语义未动（diff 中三函数无 hunks）。                                                                                                                                                                                            |

## 补充观察（不阻塞）

- 核验初始首次 `git status` 曾见 `M src/app/ai/pi-backend/image-gen/provider.ts`（T66 族 response_format 移除，非 T74 内容），核验中途该修改消失（工作树有并发进程活动）；核验结论所依据的最终工作树已不含该文件改动。T74 域文件始终为 runtime.ts + 两测试 + zones.json + T74 docs，无越界。
- 两份 untracked `docs/rebuild/records/review-2026-09-01-*.md` 为非 T74 既有遗留（∈ ownedRoot docs/，check:zones 计入 owned adds），T74-self-check 未申报，与本任务无交集。
- 小语义备注：retry 循环内 `server = await dependencies.spawn()` 每次尝试都会重赋值 server 句柄；dev 路径 spawn 返回 `{disconnect: noop}` 无资源泄漏，全失败时 disconnectCurrentServer 只清最后一次句柄——与旧行为一致，非 T74 引入。
- lint 7 warnings 抽查为 packages/core/src/tools/fork/marketing/brief.ts 等既有 max-lines，T74 三文件零警告。

## 总结论

**PASS（7/7）**——根因对症、重试语义、测试钉扎、门禁、zones 登记、自检申报、边界七项全部合规，与 T74-plan 及 T74-self-check 申报一致。
